FROM python:3.12-slim

WORKDIR /app

COPY apps/api/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY apps/api ./apps/api
COPY packages ./packages

ENV PYTHONPATH=/app

CMD ["arq", "apps.api.worker.settings.WorkerSettings"]
